# Wild Card Exports Demo

A TypeScript monorepo demonstrating wildcard export patterns and related issues.

## Structure

- `packages/library` - Library package with component and utilities
- `packages/app` - Application consuming the library

## Setup

```bash
npm install
```
